@model IEnumerable<Rubbish.Models.PickupSite>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Address.StreetNumber)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Customer.UserID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DayOfWeekID)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsActive)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RouteNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Address.StreetNumber)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Customer.UserID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DayOfWeekID)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsActive)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RouteNumber)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
